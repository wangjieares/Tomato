<resources>

    <!-- Base application theme. -->
    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">-->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="linearlayout_activity_main">
        <item name="android:background">#FFFFFF</item>
    </style>

    <style name="left_button_activity_main">
        <item name="android:src">@drawable/todo_one</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:background">#FFFFFF</item>
    </style>

    <style name="right_button_activity_main">
        <item name="android:src">@drawable/todo_one</item>
        <item name="android:scaleType">centerInside</item>
        <item name="android:layout_marginTop">0dp</item>
        <item name="android:background">#FFFFFF</item>
    </style>

    <string name="navigation_drawer_open">Open navigation drawer</string>
    <string name="navigation_drawer_close">Close navigation drawer</string>
    <!--浮动Activity的样式-->
    <style name="Theme.Transparent" parent="android:Theme.Dialog">
        <item name="android:windowIsTranslucent">true</item>//此对话框的背景
        <item name="android:windowBackground">@android:color/transparent</item>//对话框是否透明
        <item name="android:windowContentOverlay">@null</item>//对话框是否有遮盖
        <item name="android:windowNoTitle">true</item>//对话框无标题
        <item name="android:windowIsFloating">false</item>
        //对话框是否浮动
        <item name="android:backgroundDimEnabled">true</item>//是否允许对话框的背景变暗
    </style>

    <style name="pop_window_anim" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/pop_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/pop_exit_anim</item>
    </style>
    <style name="pop_window_button" parent="android:DeviceDefault.Light.ButtonBar">
            <item name="android:background">@drawable/pop_window_button_selector</item>
    </style>
    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
            <item name="android:windowIsFloating">true</item>
            <item name="android:windowIsTranslucent">false</item>
            <item name="android:windowNoTitle">true</item>
            <item name="android:background">@drawable/linear_radius</item>
            <item name="android:backgroundDimEnabled">true</item>
            <item name="android:windowBackground"> @android:color/transparent </item>
    </style>
    <style name="activity_add_item_button">
            <!--<item name="android:background">@drawable/activity_add_item_button_selector</item>-->
    </style>
</resources>
